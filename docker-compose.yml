version: '3.8'

services:
  moreai-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts:/app/artifacts
    restart: unless-stopped

networks:
  proxy-network:
    external: true
